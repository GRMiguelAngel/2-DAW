export default class Champion {

constructor(data) {
    this.name = data.name;                          // Champion's name
    this.id = data.id;                              // Champion's id
    }
}