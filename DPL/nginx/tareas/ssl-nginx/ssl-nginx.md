# Instalar ssl en los host virtuales de nginx

* se instala certbot.
```
sudo apt install certbot python3-certbot-nginx
```
```
sudo certbot --nginx
```
```
sudo certbot --nginx
```
```
sudo certbot --nginx
```